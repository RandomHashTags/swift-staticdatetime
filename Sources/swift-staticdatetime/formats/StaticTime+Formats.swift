
// MARK: ISO 8601
extension StaticTime { // TODO: support
    @inlinable
    public init?(iso8601: some StringProtocol) {
        let values = iso8601.split(separator: ":")
        return nil
    }
}